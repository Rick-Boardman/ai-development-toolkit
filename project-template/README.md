# .project (Project Memory)

This directory is the committed memory + machine contract for this project.

## Snapshot

- Project goal: [1 sentence]
- Current focus: [what we're doing this week]
- Non-goals: [explicitly out of scope]
- Constraints: [stack/version, deadlines, policies]

## How to work

- Read `now.md` first.
- Check `reminders.md` for pending reminders.
- If you hit errors, log them in `attempts.md` before retrying.
- Use `interrupt.md` as a human pause button.

## When a session ends

If work is in-flight, write a detailed dump in `handoff.md`.

_Last updated_: [YYYY-MM-DD]
